---
# Traefik IngressRoute for PiHole Web UI
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pihole-ingress
  namespace: homelab
  labels:
    app: pihole
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`pihole.sandharlanden.maerz`)
      kind: Rule
      services:
        - name: pihole-web
          port: 80

---
# Traefik IngressRoute for Emby
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: emby-ingress
  namespace: homelab
  labels:
    app: emby
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`emby.sandharlanden.maerz`)
      kind: Rule
      services:
        - name: emby
          port: 8096

---
# Traefik IngressRoute for JDownloader
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: jdownloader-ingress
  namespace: homelab
  labels:
    app: jdownloader
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`jdownloader.sandharlanden.maerz`)
      kind: Rule
      services:
        - name: jdownloader
          port: 5800

---
# Traefik IngressRoute for Sonarr
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr-ingress
  namespace: homelab
  labels:
    app: sonarr
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`sonarr.sandharlanden.maerz`)
      kind: Rule
      services:
        - name: sonarr
          port: 8989

---
# Traefik IngressRoute for Radarr
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: radarr-ingress
  namespace: homelab
  labels:
    app: radarr
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`radarr.sandharlanden.maerz`)
      kind: Rule
      services:
        - name: radarr
          port: 7878

---
# Alternative: Standard Kubernetes Ingress (if not using Traefik CRDs)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: homelab-ingress
  namespace: homelab
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - host: pihole.sandharlanden.maerz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pihole-web
                port:
                  number: 80
    - host: emby.sandharlanden.maerz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: emby
                port:
                  number: 8096
    - host: jdownloader.sandharlanden.maerz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jdownloader
                port:
                  number: 5800
    - host: sonarr.sandharlanden.maerz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sonarr
                port:
                  number: 8989
    - host: radarr.sandharlanden.maerz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: radarr
                port:
                  number: 7878
