apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: homelab
  labels:
    app: sonarr
spec:
  type: NodePort
  ports:
    - name: http
      port: 8989
      targetPort: 8989
      nodePort: 30989
      protocol: TCP
  selector:
    app: sonarr
